#include <stdio.h>
 
int main() {
    int i = 1; // variável de incremento
   
    while (i <= 5) { // condição de parada
        printf("%d\n", i); // imprime o valor atual de i - Saida
        i++; // incrementa 1 em i até ele chegar a 5
    }
   return 0;
}