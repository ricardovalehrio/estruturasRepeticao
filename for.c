#include <stdio.h>
 
int main() {
    for (int i = 1; i <= 5; i++) { // inicialização; condição; incremento; a variável i é declarada dentro do for
        printf("%d\n", i);
    }
   
    return 0;
}